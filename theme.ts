import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: "#06283D",
    secondary: "#1363DF",
    tertiary: "#47B5FF",
    "blue-sky": "#DFF6FF",
    gold: "#F6BD60",
  },
});

export default theme
